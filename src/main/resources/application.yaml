app:
  rest-template:
    baseUrlForEvents: "https://kudago.com/public-api/v1.4"
    baseUrlForConvertValute: "http://localhost:4040"
  reactive-client:
    baseUrlForEvents: "https://kudago.com/public-api/v1.4"
    baseUrlForConvertValute: "http://localhost:4040"


api:
  client:
    reactive:
      enabled: true

spring:
  application:
    name: Fintech_Spring


thread:
  threadCount: 16

duration:
  delay: 10

server:
  port: 8080

